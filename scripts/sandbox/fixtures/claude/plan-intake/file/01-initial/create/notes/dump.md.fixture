# Brain dump

Unsorted, written fast, nothing checked against the code.

- every command prints straight to the console and we shipped a log module that I think nobody imports
- build and serve each parse their own flags with a hand-rolled loop, that has to be one helper
- error messages are thrown as bare strings from wherever, no shape to them at all
- logs should go to stderr so piping stays clean
- do I want the log module to own color output, or is that a formatter's job? genuinely unsure
- switching the whole CLI onto a real argument parser instead of the loops, no idea what that costs until someone builds it three ways and measures the bundle
- the error envelope task on the board might already own half the error mess
