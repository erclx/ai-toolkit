export function main(argv: string[]) {
  const command = argv[2]

  if (command === 'build') {
    process.stderr.write('building\n')
    return
  }

  process.stderr.write('usage: sbx build\n')
  process.exit(1)
}
