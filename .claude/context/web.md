---
title: Web
description: The canon.erclx.dev landing page, its Astro build, and the CI and layout gaps that follow from that
---

# Web

## Overview

Owns `web/`, the Astro app behind the `canon.erclx.dev` landing page: one route, eight page sections. This is the toolkit's own public site rather than code shipped to a target project, so the application-code non-goal in `.claude/REQUIREMENTS.md` does not reach it. See `.claude/wireframes/landing-page.md` for layout intent and `.claude/context/tooling.md` for the astro-stack mechanics this domain inherits.

## Layout

- `web/src/pages/` owns the one route, assembling the section components
- `web/src/components/` owns the eight page sections
- `web/src/content/` owns page copy, each string tied to a `README.md` citation
- `web/src/fixtures/` owns the committed agent-view session snapshot
- `web/src/lib/` owns the build-time catalog reader: `catalogs.ts` and `counts.ts` both call the shared CLI spawn in `canon-cli.ts`
- `web/src/layouts/` owns the shared page shell
- `web/src/styles/` owns global CSS and the generated design tokens
- `web/e2e/` owns the Playwright suite
- `web/public/assets/` owns images symlinked from the repository's own `assets/`

## Decisions

- `catalogs.ts` and `counts.ts` both read through `readCanonJson` in `canon-cli.ts` rather than each spawning `canon` on PATH. A PATH-resolved `canon` can be a different install than the checkout the build is running from, so a build in a linked worktree could read the main checkout's catalogs while the rest of the page rendered from the branch. The shared reader spawns `bun` against this checkout's own `src/cli.ts`, resolved through `repoRoot()`'s `git rev-parse --show-toplevel`, the shape `web:tokens` already took in `package.json`. It checks `stdout` rather than the exit code, since `canon gov counts` can exit 2 for drift elsewhere in the tree that this page's build did not cause, so throwing on any non-zero exit would fail the build on a problem this page cannot see.
- Every string in `copy.ts` carries an inline citation to `README.md` or the CLI verb it derives from, gated by `readmeCitations` in `src/gate/measures.ts`. The citation discipline itself is stated in `internal/rules/claude/593-landing-page.md`.
- `global.css`'s defaults sit inside `@layer base`, not unlayered. Tailwind v4 generates its utilities inside `@layer utilities`, where an unlayered rule always wins regardless of specificity. An unlayered default once beat the CTA buttons' `text-(--color-*)` utility outright and rendered both with invisible label text, caught by screenshot review before merge.
- No `web/package.json` exists, since the repository declares no Bun workspaces. `bun run` invoked from elsewhere walks up to the repository root to run `web:build` and `web:preview`, pulling Playwright's cwd-relative output defaults with it, so `web/playwright.config.ts` anchors `outputDir` and its report folder to its own directory instead (`fileURLToPath(new URL('.', import.meta.url))`).
- Every `web:*` script in `package.json` runs `cd web` before invoking astro, rather than passing `astro --root web`. The mechanism, the golden astro config's `@` alias resolving against the invoking process's cwd, is recorded in `.claude/context/tooling.md`'s Gotchas. This is that domain's instance of the choice.
- `agent-view.astro` re-creates Claude Code's own session list rather than illustrating it, so its rows come from `web/src/fixtures/agent-view.json`, written by `scripts/core/regen-agent-fixture.sh` off a live `canon sessions list --json` read. The generator refuses rather than writing a row it cannot see running, which is what stops the section inventing its own subject. Two earlier shapes were wrong in ways every check here passed: one band label that swapped in place, where the surface stands three bands at once, and a branch filter that excluded the orchestrator and every planner from a section about a dispatch, since a controlling session and a planning session both sit on the trunk. The activity text and the pull request number stay transcribed, because no verb reports the first and nothing joins a session record to the second.
- `web/public/assets/*.png` and the favicon are symlinks into the repository's own `assets/`, not copies, for one image source. The Windows-checkout cost this carries, materializing each as a plain-text path with no build error, is recorded in `.claude/context/tooling.md`'s Gotchas.
- The favicon is no longer a symlink. `web/public/assets/*.png` still are, and `web/public/favicon.svg` is written by `scripts/core/regen-web-favicon.ts` from `assets/brand/mark.svg` and the two accent values in `web/src/styles/tokens.css`. The source keeps `currentColor` because the nav embeds the same mark inline and wants it to inherit, and a favicon loads with no CSS context, so the symlinked source resolved `currentColor` against the initial value of `color` and painted the tab icon pure black on every surface. This is the fourth copy of the mark and the only one that can answer for itself: `regen-hero.sh` and `src/design/render.ts` each bake a single literal because both embed the mark as a data URI, while a file served at its own URL carries a `prefers-color-scheme` branch and tracks the reader's theme.
- The `design` gate stage regenerates and drift-checks `web/src/styles/tokens.css` and `web/public/favicon.svg` alongside `.claude/DESIGN.md` and `src/design/base.css`. Before this, a value moved in `src/design/tokens.ts` could leave the web stylesheet and the tab icon stale with the gate reporting green, since neither ran through `web:tokens` or `web:favicon` as part of `bun run check`.
- Four of the five committed rasters under `web/public/assets/` came out once the sections they illustrated moved to live DOM. `hero.png` alone stays, since the card image still needs a raster and nothing else on the page renders one. The four still exist at `assets/*.png` for `README.md`, which keeps them under `regen-hero.sh`'s own gate.
- Every section is rendered from a build-time catalog read rather than from a committed image. `web/src/lib/catalogs.ts` reads `canon gov list`, `canon tooling list`, and `canon standards list` through the shared reader, throwing rather than shipping a literal. The reader raises `maxBuffer` because the standards catalog carries every standard's full body. The rules panel names every domain with its true count and samples the rules inside each, because a flat sample of ten renders five of the eight domains and gives a reader no way to know the other three exist.
- `home.spec.ts`'s path-scoped-rule test reads the same catalog through `readRuleGroups` and `loadsWhen` rather than asserting today's first glob as a literal. A rule's `paths:` list edited on another branch changes which glob the sampler reaches first, and a hardcoded string failed on that alone with nothing about the page actually wrong.
- `astro.config.mjs`'s `site` value imports `site.origin` from `content/copy.ts` rather than reading its own `ASTRO_SITE` environment variable. The two disagreed: `copy.ts` carries the real deployed origin, which `og:url` and the card image already depend on, while the config's env-var fallback pointed at a reserved invalid host that `deploy-site.yml` never overrides, since nothing consumes `Astro.site` yet. One import closes that gap rather than wiring the workflow to set the variable, since the origin `copy.ts` already carries is the one that is actually correct.
- The page speaks in one typeface. A measurement across the built page found the proportional family in exactly one of 216 text elements, and a single element in a family nothing else uses is what read as belonging to another design. The same fault reached the primary button twice first: `.page-display` sat on the hero section and cascaded its family into the subhead and the call to action, leaving the page's two primary buttons in different typefaces. Giving the interface a proportional voice throughout, with mono kept for what is literally code, is a larger decision this one does not take.
- `--nav-height` is declared in `global.css` only as a fallback. `site-nav.astro` measures the bar it rendered and republishes the value, with a `ResizeObserver` re-measuring when the links wrap out below the small breakpoint, because `scroll-padding-top` derives an anchor's landing from it and a hand-written height was already 8px wrong when first committed. Before that property was declared at all, anchor landing worked by accident: every section carries 96px of top padding, which happened to exceed the bar, so a heading cleared it and a section with less padding would have landed underneath with nothing reporting it.

## Gotchas

- `web:tokens` and `web:favicon` invoke `bun src/cli.ts`, never the globally linked `canon`. A global binary resolves to the main checkout whatever worktree runs it, so `web:build` once overwrote a regenerated `web/src/styles/tokens.css` with output from a CLI that lacked the branch's own token changes. The revert was silent and surfaced only when a staged diff was read. `scripts/core/regen-hero.sh` documents the same trap in its own header.
- A design-token change moves the capture markup and the stamps and leaves the PNGs byte-identical, because the frames render on the dark ground and never set `data-theme`, so an added light role is inert in them. The Hero stage is a drift check over `assets/captures/*.html`, so it fails until the regenerated frames are committed, which is the stage working rather than a defect.
- `web/` has no typecheck in the automated gate, in three layers. `src/gate/stages.ts`'s `types` stage scopes to `/^src\/|^tsconfig\.json$|^package\.json$/`, so a web-only diff never matches it and the stage reports Skipped, and the root `tsconfig.json` sets `include` to `["src"]` alone, so `check:types` would not read `web/` even if the stage ran. `web/tsconfig.json` extends `astro/tsconfigs/strict`, but only the manual `bun run web:build`, through `astro check`, ever type-checks against it.
