Show each proposed edit as an old → new pair.

For each edit:

- **File:** `<path>` → `<section or symbol>`. Use the markdown heading for prose, the function or class name for code. Skip line numbers since they go stale.
- Label `Old:` then quote the existing text as a `>` blockquote.
- Label `New:` then quote the proposed text as a `>` blockquote.

Group multiple edits in the same file under one File header, one location header per edit.
