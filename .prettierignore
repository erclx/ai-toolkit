.claude/GOV.md

# release-please rewrites these on every release and serializes them its own way
**/.claude-plugin/*.json
/CHANGELOG.md

# hand-authored, no tool writes it, so it keeps its format gate
!.claude-plugin/marketplace.json

# regen-hero.sh writes this and serializes it its own way. Formatting it too
# gives the file two writers that disagree, and the Hero stage then
# fails for whichever of them committed last rather than for a stale count.
# The template beside it is hand-edited and keeps its format gate.
/assets/hero.html
