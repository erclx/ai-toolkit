<!doctype html>
<!--
  Generated by scripts/core/regen-hero.sh from assets/showcase-governance.html.tmpl.
  Do not edit this file. The token block below is filled from
  `canon design css --no-components`, so a value moved in src/design/tokens.ts
  moves this frame and `bun run check` fails on the difference.

  Source for showcase-governance.png. Every stack row, rule name, and path glob
  below is filled from `canon gov list --json` at build time rather than typed,
  so a stack gaining a rule or a rule changing its glob moves this frame on the
  next run. Nothing here is hand-maintained except the command line and the
  frame chrome.

  The rule sample is drawn evenly across the stack-reached catalog rather than
  taking its first rows, matching how the hero samples. A rule no stack names is
  opt-in behind `--add`, so featuring one would advertise an entry no ordinary
  `canon gov install` delivers.

  What this frame is for: the README claims governance rules load into a session
  when a matching path is edited, and shows nothing. The path column is that
  claim made visible, which is why the glob is rendered here and the description
  is not.
-->
<html>
  <head>
    <meta charset="utf-8" />
    <style>
{{TOKENS}}

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background: transparent;
        padding: 28px;
        font-family: 'Noto Sans Mono', 'DejaVu Sans Mono', monospace;
      }
      .window {
        width: 880px;
        background: var(--color-background);
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 18px 48px rgba(0, 0, 0, 0.45);
      }
      .titlebar {
        height: 34px;
        background: var(--color-chrome);
        display: flex;
        align-items: center;
        padding: 0 14px;
        gap: 8px;
      }
      .dot {
        width: 11px;
        height: 11px;
        border-radius: 50%;
      }

      /* The three dots imitate somebody else's window chrome rather than
         speaking this system's palette, so they stay literal, exactly as
         install.html.tmpl states for the same three. */
      .red {
        background: #ec6a5e;
      }
      .yellow {
        background: #f4bf50;
      }
      .green {
        background: #61c454;
      }
      .title {
        flex: 1;
        text-align: center;
        color: var(--color-muted);
        font-size: 12px;
        letter-spacing: 0.02em;
        margin-right: 46px;
      }
      pre {
        padding: 20px 22px 24px;
        font-size: 13px;
        line-height: 1.6;
        color: var(--color-text-body);
        white-space: pre;
      }
      .prompt {
        color: var(--color-accent);
      }
      .cmd {
        color: var(--color-text);
        font-weight: 600;
      }
      .muted {
        color: var(--color-muted);
      }
      .ok {
        color: var(--color-success);
      }
      /* The border role is tuned for a rule drawn against a panel and reads as
         almost nothing at this size, so the frame glyphs take the muted role
         instead. They are structure a reader follows down the left edge rather
         than an edge between two surfaces. */
      .frame {
        color: var(--color-muted);
      }
      .name {
        color: var(--color-text);
      }
      .domain {
        color: var(--color-accent);
      }
    </style>
  </head>
  <body>
    <div class="window">
      <div class="titlebar">
        <span class="dot red"></span>
        <span class="dot yellow"></span>
        <span class="dot green"></span>
        <span class="title">canon gov list</span>
      </div>
      <pre><span class="prompt">$</span> <span class="cmd">canon gov list</span>
<span class="frame">┌</span>
<span class="frame">│</span> canon gov list
<span class="frame">│</span>
<span class="frame">├</span> Stacks <span class="muted">({{GOV_STACK_COUNT}})</span>
{{GOV_STACK_ROWS}}
<span class="frame">│</span>
<span class="frame">├</span> Rules <span class="muted">({{RULE_COUNT}} authored, each loading only when its glob matches the edited path)</span>
{{GOV_RULE_ROWS}}
<span class="frame">│</span> <span class="muted">+{{RULE_MORE}} more</span>
<span class="frame">└</span></pre>
    </div>
  </body>
</html>
